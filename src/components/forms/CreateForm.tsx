import style from "@/styles/login.module.css";

export const CreateForm = () => {
  return (
    <div className={style.body}>
      <div className={`${style.form} w-full`}>CreateForm</div>
    </div>
  );
};
